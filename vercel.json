{
    "version": 2,
    "builds": [{ "src": "./index.js", "use": "@vercel/node" }],
    "routes": [
        { "src": "/:color", "dest": "/:color" },
        { "src": "/:color?code=:code", "dest": "/:color?code=:code" },
        { "src": "/(.*)", "dest": "/" }
    ]
}
